<h1>GeekTwitter</h1>
<h3>Tweet詳細</h3>

<div class="tweet">
  <p><%= @makura.body %></p>
  <p><%= @makura.name %></p>
  <p><%= @makura.created_at %></p>
</div>

<%= link_to "Tweet一覧に戻る", makuras_path %>